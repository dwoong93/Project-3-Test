{{#extends 'base'}}

{{#block 'content'}}
<h1>Welcome Admin, Add a new Keycap</h1>
<form method="POST">
    {{{form}}}
    <input type="hidden" name="_csrf" value="{{csrfToken}}"/>
    <input type="submit" value="Add Product" class="btn btn-primary"/>
    <a href="/products/catalog" class="btn btn-outline-success">Cancel</a>
</form>
{{/block}}

{{/extends}}